export interface filterConfig {
  company?: boolean;
  customer?: boolean;
  center?: boolean;
  unit?: boolean;
  shift?: boolean;
  typeworker?: boolean;
  fromDate?: boolean;
  toDate?: boolean;
}
