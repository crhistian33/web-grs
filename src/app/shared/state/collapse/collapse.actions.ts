export namespace CollapseAction {
  export class toggleNavUser {
    static readonly type = '[Collapse] Toggle nav user';
  }

  export class closeNavUser {
    static readonly type = '[Collapse] Close nav user';
  }

  export class toggleFilter {
    static readonly type = '[Collapse] Toggle filter';
  }

  export class closeFilter {
    static readonly type = '[Collapse] Close filter';
  }
}
